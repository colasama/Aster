name: Build desktop artifacts

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: desktop-artifacts-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: "1"

jobs:
  quality:
    name: Quality gates
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.97.1
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: desktop-artifacts-quality
      - run: pnpm install --frozen-lockfile
      - name: Check Rust formatting and lint
        run: pnpm exec lefthook run pre-commit --force --command rustfmt --command clippy
      - name: Check frontend and Rust tests
        run: pnpm exec lefthook run pre-push --force
      - name: Check dependency policy
        uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check

  bundle:
    name: Bundle ${{ matrix.name }}
    needs: quality
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x64
            os: ubuntu-22.04
            builder_args: --linux AppImage deb --x64
            artifact: aster-linux-x64
            binary_dir: release/linux-unpacked/resources/bin
          - name: Windows x64
            os: windows-latest
            builder_args: --win nsis --x64
            artifact: aster-windows-x64
            binary_dir: release/win-unpacked/resources/bin
          - name: macOS arm64
            os: macos-15
            builder_args: --mac dmg zip --arm64 -c.mac.identity=- -c.mac.hardenedRuntime=false
            artifact: aster-macos-arm64
            binary_dir: release/mac-arm64/Aster.app/Contents/Resources/bin
            rust_target: aarch64-apple-darwin
            ffmpeg_args: --target darwin-arm64
          - name: macOS x64
            os: macos-15
            builder_args: --mac dmg zip --x64 -c.mac.identity=- -c.mac.hardenedRuntime=false
            artifact: aster-macos-x64
            binary_dir: release/mac/Aster.app/Contents/Resources/bin
            rust_target: x86_64-apple-darwin
            ffmpeg_args: --target darwin-x64
            verify_args: --checksums-only
    runs-on: ${{ matrix.os }}
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.97.1
      - name: Install Apple Rust target
        if: runner.os == 'macOS'
        run: rustup target add ${{ matrix.rust_target }}
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: desktop-artifacts-${{ runner.os }}
      - run: pnpm install --frozen-lockfile
      - name: Install Linux packaging tools
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y libarchive-tools
      - uses: actions/cache@v4
        with:
          path: |
            .aster-cache/ffmpeg/*.gz
            .aster-cache/ffmpeg/*.zip
            .aster-cache/ffmpeg/*.LICENSE
            .aster-cache/ffmpeg/*.README
          key: ffmpeg-${{ runner.os }}-${{ hashFiles('scripts/ffmpeg-downloads.json') }}
      - name: Download and stage pinned FFmpeg and FFprobe
        run: pnpm artifact:download-ffmpeg ${{ matrix.ffmpeg_args }}
      - name: Build frontend and native bridge
        if: runner.os != 'macOS'
        run: pnpm build
      - name: Build macOS assets and bridge for ${{ matrix.rust_target }}
        if: runner.os == 'macOS'
        shell: bash
        run: |
          pnpm typecheck
          pnpm exec vite build
          pnpm electron:compile
          cargo build --release -p aster-desktop-bridge --target ${{ matrix.rust_target }}
          mkdir -p target/release
          cp target/${{ matrix.rust_target }}/release/aster-desktop-bridge target/release/
          cp target/${{ matrix.rust_target }}/release/aster-mcp target/release/
          codesign --force --sign - target/release/aster-desktop-bridge
          codesign --force --sign - target/release/aster-mcp
      - name: Package desktop bundles
        run: pnpm exec electron-builder --config scripts/electron-builder.ts --publish never ${{ matrix.builder_args }}
      - name: Verify packaged tools and generate checksums
        run: node scripts/verify-desktop-bundle.mjs "${{ matrix.binary_dir }}" ${{ matrix.verify_args }}
      - name: Upload desktop bundles
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}-${{ github.sha }}
          path: |
            release/*.exe
            release/*.dmg
            release/*.zip
            release/*.AppImage
            release/*.deb
            release/SHA256SUMS.txt
          if-no-files-found: error
          retention-days: 14
          compression-level: 0
